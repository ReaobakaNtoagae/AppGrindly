<resources>

    <!-- Material 3 Base Theme -->
    <style name="Theme.GrindlyApp1" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorOnPrimary">@color/black</item>

        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>

    <!-- No Action Bar -->
    <style name="Theme.GrindlyApp1.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_200</item>
    </style>

    <!-- REQUIRED FOR app_bar_main.xml -->
    <style name="Theme.GrindlyApp1.AppBarOverlay" parent="ThemeOverlay.Material3.ActionBar" />

    <style name="Theme.GrindlyApp1.PopupOverlay" parent="ThemeOverlay.Material3.Light" />

</resources>
